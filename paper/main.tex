\documentclass{article}

\usepackage{microtype}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}

\newcommand{\theHalgorithm}{\arabic{algorithm}}
\usepackage{icml2026}

\InputIfFileExists{values.tex}{}{}
\input{model.tex}

\icmltitlerunning{\smash{Context Position and Pruning in a Small Language Model}}

\begin{document}

\twocolumn[
\icmltitle{Answer Position and Lexical Pruning Shape Synthetic Clause Retrieval\
           in a Small Language Model}
\icmlsetsymbol{equal}{*}
\begin{icmlauthorlist}
\icmlauthor{Anonymous Authors}{anon}
\end{icmlauthorlist}
\icmlaffiliation{anon}{Anonymous Institution}
\icmlcorrespondingauthor{Anonymous Author}{anon.email@domain.com}
\icmlkeywords{long context, retrieval, position effects, small language models}
\vskip 0.3in
]
\printAffiliationsAndNotice{}

\begin{abstract}
Does adding context hurt a small language model, and do the location and composition of that context matter? We evaluate the frozen \modelName{} artifact on \sampleSize{} synthetic contract-clause probes. Deterministic runs span mean prompt lengths of \tierTwoTokens{}, \tierFourTokens{}, and \tierEightTokens{} tokens. Moving the answer clause from the start to the end raised accuracy by \positionTwoStartEndChange{} points at the shortest tier and \positionFourStartEndChange{} points at the middle tier. Keyword pruning changed matched accuracy by \pruningChange{} points, whereas selecting sentences that named the question's company reached \entitySelectorAccuracy{}\% accuracy and improved on keyword pruning by \selectorComparisonChange{} points. The matched comparison across prompt lengths was inconclusive, and the filler manipulation does not isolate topical relevance.
\end{abstract}

\section{Introduction}

You paste a long contract into a chatbot and ask, ``What code renews the vendor agreement?'' The answer sits in one clause, but a similar buyer clause repeats the words in your question and gives a different code.

This ordinary lookup becomes less predictable in a long prompt. The model may fail because it must process a longer document, because the answer appears in a weak position, or because nearby text resembles the question. Each explanation suggests a different response: shorten the prompt, move the key clause, or filter the document. Distinguishing among them requires an evaluation that varies a single property while holding the others fixed.

We study these choices in a synthetic, gradeable version of the contract situation. Each document contains an answer-bearing vendor clause and a look-alike clause for another company. The question uses the wording of the look-alike clause, while the answer clause expresses the same relation with different words. This construction reduces the value of literal matching, following the motivation of NoLiMa \citep{modarressi2025nolima}.

We provide a paired evaluation of a frozen open model at laptop scale. Prior work established position and length failures in larger systems \citep{liu2023lost,levy2024same} and showed that irrelevant statements can disrupt reasoning \citep{shi2023distracted}. Our comparisons match prompt lengths, answer positions, and questions while varying whether the filler is code-heavy business text or unrelated prose. We compare a deterministic keyword filter with a query-entity selector that keeps sentences naming the company in the question.

The experiment asks four questions:
\begin{enumerate}
    \item As mean prompt length grows from \tierTwoTokens{} to \tierEightTokens{} tokens, how much does accuracy change on shared cases?
    \item How does moving the same answer clause among the start, middle, and end change accuracy?
    \item How does accuracy differ between code-heavy business filler and unrelated prose in matched documents?
    \item Which cheap selector better preserves accuracy: keyword overlap or matching the company named in the question?
\end{enumerate}

The experiment does not identify a general context-length threshold. Within this grid, accuracy was higher when the answer appeared near the end and lower with code-heavy business filler than with unrelated prose. Keyword pruning amplified the lexical trap, while matching the query entity avoided it.

\section{Related Work}

\citet{liu2023lost} found a U-shaped position curve in multi-document question answering and key-value retrieval, with stronger use of information near context boundaries. We conduct a smaller clause-reordering test in which the question, filler sequence, clauses, and codes remain fixed while we insert the answer clause at a different depth. When we place it before the competing clause, the competing clause's final sentence index shifts by one. The resulting pattern mainly favors recent information rather than forming a full U-shape, because accuracy is also low in the start condition.

Prompt length can matter even when the underlying problem is fixed. \citet{levy2024same} padded matched reasoning problems and observed degradation well before model context limits, while RULER found that usable context lengths can be shorter than advertised windows \citep{hsieh2024ruler}. Our prompt-length comparison includes only cases present at every tier, so the mix of scenarios remains fixed as prompts grow.

\citet{shi2023distracted} added irrelevant statements to arithmetic problems and found lower reasoning accuracy. We compare filler from the same business domain with unrelated prose while matching prompt length and question wording. The business condition also contains more entities and code-shaped candidates, so the comparison measures that bundle rather than topical relevance alone. NoLiMa shows that synthetic retrieval tests based on literal word matches can overstate retrieval ability \citep{modarressi2025nolima}. We therefore paraphrase the question in the answer clause while giving the competing clause the same surface wording as the question. The tested model belongs to the instruction-tuned Qwen family \citep{qwen2024qwen25}.

\section{Probe Design}

\paragraph{Model and execution.}
We evaluate the frozen \modelFile{} artifact, whose SHA checksum appears below:
\par\noindent\resizebox{\linewidth}{!}{\modelSha}\par
We run it through \texttt{llama-completion} with greedy decoding, zero temperature, and a fixed seed. The complete manifest contains \manifestProbeCount{} probes, each stored as one prompt and one raw output. Existing outputs are skipped on rerun, so interrupted inference resumes by stable probe identifier.

\paragraph{Scenario families.}
For each family, seeded code generates the target company and code, a competing company and code, a question, and filler. The question asks for the renewal code needed to extend the target company's contract. Its wording exactly matches the competing clause, whereas the answer-bearing clause calls the same value a renewal identifier. A matched literal control reverses the wording of the two clauses while retaining the same filler.

For each full-context family, we insert the answer clause near the start, middle, or end. Family generation does not use target position in any random seed. We assign the competing clause a fractional insertion index, either earlier or later in the document, that is independent of answer position and fixed across the three versions. Its final sentence index shifts by one when the answer is inserted before it. Thus, each paired position contrast changes both the answer placement and this mechanical one-sentence shift while preserving the scenario content and filler order.

\paragraph{Amount and filler composition.}
The tokenizer, rather than a character estimate, determines prompt length. The three full-context tiers average \tierTwoTokens{}, \tierFourTokens{}, and \tierEightTokens{} tokens. Each tier balances business filler with unrelated sentences. Business filler consists of other companies, records, and unrelated codes; unrelated filler describes places and events outside the contract domain. Filler pairs share the family, target position, question, and measured tier.

This manipulation changes several properties together. Compared with unrelated prose, the related filler contains more business entities, contract-like templates, and code-shaped answer candidates. The paired comparison therefore identifies the effect of these two generated filler conditions, not the effect of topical relevance alone.

The final tier contains fewer families because it is more expensive to run. Prompt-length estimates therefore use only the \amountSharedEightCount{} cases shared by every tier rather than the unequal full-tier totals. Filler-composition estimates use \qualityTwoRelevantDenominator{}, \qualityFourRelevantDenominator{}, and \qualityEightRelevantDenominator{} pairs at the three tiers, respectively.

\paragraph{Selectors and controls.}
At the middle tier, a keyword filter retains sentences with at least two content words shared with the question, falling back to the highest-overlap sentences if too few qualify. A second selector extracts the company named after ``contract with'' in the question and keeps sentences containing that name. If extraction or matching fails, it falls back to the keyword filter. Both rules use only visible text, preserve original order, and require no answer labels. The comparisons are restricted to the \pruningDenominator{} cases with full context and both selector outputs. We also include \closedBookCount{} closed-book questions and \literalControlDenominator{} matched literal-versus-paraphrase controls.

The keyword filter retained the answer clause and lexical decoy in all \pruningPrunedCount{} matched prompts. The query-entity selector retained the answer clause in \entitySelectorTargetRetainedCount{} prompts and the decoy in \entitySelectorDistractorRetainedCount{}. A paired diagnostic removes only the decoy from each keyword-pruned prompt, leaving every other selected sentence fixed. This diagnostic uses the known synthetic label; both automatic selectors do not.

\section{Scoring and Comparisons}

Normalized accuracy requires the target code to appear without the competing code. Exact-output compliance requires a response containing one code and no other text. A distractor capture contains the competing code without the target, and an invalid response contains both expected codes or neither. We strip only the runner's fixed transport trailer before applying these rules.

The inferential target is the population of synthetic scenario families defined by the seeded generator. To compute each \confidenceLevel{}\% interval, we resample whole observed families while retaining all positions, filler types, and variants from every selected family. Exact enumeration makes this cluster bootstrap deterministic. Paired changes are differences in accuracy over an exact intersection of scenario keys, and their intervals use the same family-level resampling. The intervals describe sensitivity to the small sample of families, while wins, losses, and ties expose the underlying paired outcomes. Because greedy decoding is deterministic, the intervals do not measure uncertainty from repeated decoding.

\section{Results}

\paragraph{Amount.}
\textbf{The relationship between prompt length and accuracy remains unresolved.} Accuracy was \amountSharedTwoAccuracy{}\% at a mean prompt length of \tierTwoTokens{} tokens and \amountSharedFourAccuracy{}\% at \tierFourTokens{} tokens. This paired change of \amountSharedTwoFourChange{} points has a family-cluster interval of [\amountSharedTwoFourChangeLow{}, \amountSharedTwoFourChangeHigh{}] over \amountSharedTwoFourDenominator{} cases. At \tierEightTokens{} tokens, accuracy was \amountSharedEightAccuracy{}\%, with \amountSharedFourEightWins{} wins, \amountSharedFourEightLosses{} losses, and \amountSharedFourEightTies{} ties relative to the middle tier (Table~\ref{tab:amount-quality}). Because the longest-tier intersection represents only \amountSharedEightCount{} probes from \amountSharedEightFamilyCount{} scenario families, these observations neither establish a benefit nor rule out degradation within the measured range.

\begin{table}[t]
\caption{Matched amount and filler-composition comparisons. Brackets give \confidenceLevel{}\% family-cluster bootstrap intervals; $n$ is the matched denominator. Changes are later minus earlier for amount and business filler minus unrelated prose for composition.}
\label{tab:amount-quality}
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lccc@{}}
\toprule
Comparison & Accuracy A & Accuracy B & Change ($n$) \\
\midrule
Amount: short to middle
& \amountSharedTwoAccuracy{} [\amountSharedTwoAccuracyLow{}, \amountSharedTwoAccuracyHigh{}]
& \amountSharedFourAccuracy{} [\amountSharedFourAccuracyLow{}, \amountSharedFourAccuracyHigh{}]
& \amountSharedTwoFourChange{} (\amountSharedTwoFourDenominator{}) \\
Amount: middle to long
& \amountSharedFourAccuracy{} [\amountSharedFourAccuracyLow{}, \amountSharedFourAccuracyHigh{}]
& \amountSharedEightAccuracy{} [\amountSharedEightAccuracyLow{}, \amountSharedEightAccuracyHigh{}]
& \amountSharedFourEightChange{} (\amountSharedFourEightDenominator{}) \\
\midrule
Composition at short tier
& \qualityTwoIrrelevantAccuracy{} [\qualityTwoIrrelevantAccuracyLow{}, \qualityTwoIrrelevantAccuracyHigh{}]
& \qualityTwoRelevantAccuracy{} [\qualityTwoRelevantAccuracyLow{}, \qualityTwoRelevantAccuracyHigh{}]
& \qualityTwoRelevantChange{} (\qualityTwoRelevantDenominator{}) \\
Composition at middle tier
& \qualityFourIrrelevantAccuracy{} [\qualityFourIrrelevantAccuracyLow{}, \qualityFourIrrelevantAccuracyHigh{}]
& \qualityFourRelevantAccuracy{} [\qualityFourRelevantAccuracyLow{}, \qualityFourRelevantAccuracyHigh{}]
& \qualityFourRelevantChange{} (\qualityFourRelevantDenominator{}) \\
\bottomrule
\end{tabular}
}
\end{table}

\paragraph{Position.}
\textbf{Moving the answer clause to the end produced the largest and most consistent gain.} At the shortest tier, accuracy rose from \positionTwoStartAccuracy{}\% at the start to \positionTwoEndAccuracy{}\% at the end. The paired increase was \positionTwoStartEndChange{} points, with a family-cluster interval of [\positionTwoStartEndChangeLow{}, \positionTwoStartEndChangeHigh{}], \positionTwoStartEndWins{} wins, and no losses. At the middle tier, the same move raised accuracy by \positionFourStartEndChange{} points [\positionFourStartEndChangeLow{}, \positionFourStartEndChangeHigh{}], with \positionFourStartEndWins{} wins and no losses. The sparse longest tier also favored the end over the start by \positionEightStartEndChange{} points, but it contains only \positionEightStartEndDenominator{} pairs. Accuracy with the clause in the middle was lower than accuracy with it at the end in the two populated tiers (Table~\ref{tab:position}).

\begin{table}[t]
\caption{Accuracy by answer-clause position. Each row compares the same scenario families across positions. Brackets give \confidenceLevel{}\% family-cluster bootstrap intervals.}
\label{tab:position}
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lccc@{}}
\toprule
Mean tokens & Start & Middle & End \\
\midrule
\tierTwoTokens{}
& \positionTwoStartAccuracy{} [\positionTwoStartAccuracyLow{}, \positionTwoStartAccuracyHigh{}]
& \positionTwoMiddleAccuracy{} [\positionTwoMiddleAccuracyLow{}, \positionTwoMiddleAccuracyHigh{}]
& \positionTwoEndAccuracy{} [\positionTwoEndAccuracyLow{}, \positionTwoEndAccuracyHigh{}] \\
\tierFourTokens{}
& \positionFourStartAccuracy{} [\positionFourStartAccuracyLow{}, \positionFourStartAccuracyHigh{}]
& \positionFourMiddleAccuracy{} [\positionFourMiddleAccuracyLow{}, \positionFourMiddleAccuracyHigh{}]
& \positionFourEndAccuracy{} [\positionFourEndAccuracyLow{}, \positionFourEndAccuracyHigh{}] \\
\tierEightTokens{}
& \positionEightStartAccuracy{} [\positionEightStartAccuracyLow{}, \positionEightStartAccuracyHigh{}]
& \positionEightMiddleAccuracy{} [\positionEightMiddleAccuracyLow{}, \positionEightMiddleAccuracyHigh{}]
& \positionEightEndAccuracy{} [\positionEightEndAccuracyLow{}, \positionEightEndAccuracyHigh{}] \\
\bottomrule
\end{tabular}
}
\end{table}

\paragraph{Filler composition.}
\textbf{Code-heavy business filler produced lower accuracy than unrelated prose.} The matched difference was \qualityTwoRelevantChange{} points [\qualityTwoRelevantChangeLow{}, \qualityTwoRelevantChangeHigh{}] over \qualityTwoRelevantDenominator{} pairs at the shortest tier and \qualityFourRelevantChange{} points [\qualityFourRelevantChangeLow{}, \qualityFourRelevantChangeHigh{}] over \qualityFourRelevantDenominator{} pairs at the middle tier. At the shortest tier, the paired outcomes were \qualityTwoRelevantWins{} win, \qualityTwoRelevantLosses{} losses, and \qualityTwoRelevantTies{} ties. At the middle tier, they were \qualityFourRelevantWins{} win, \qualityFourRelevantLosses{} losses, and \qualityFourRelevantTies{} ties. The longest-tier estimate was \qualityEightRelevantChange{} points, but its \qualityEightRelevantFamilyCount{} families are too sparse to strengthen that conclusion. Because the business filler also adds code-shaped candidates and repeated entity templates, these results do not show that topical relevance caused the difference.

\paragraph{Mitigation.}
\textbf{The query-entity selector reversed the keyword filter's failure.} Full-context accuracy was \pruningFullAccuracy{}\%, compared with \pruningPrunedAccuracy{}\% after keyword filtering and \entitySelectorAccuracy{}\% after query-entity selection. Query-entity selection improved on full context by \entitySelectorChange{} points [\entitySelectorChangeLow{}, \entitySelectorChangeHigh{}], with \entitySelectorWins{} wins and no losses. Compared with keyword pruning, it gained \selectorComparisonChange{} points [\selectorComparisonChangeLow{}, \selectorComparisonChangeHigh{}], with \selectorComparisonWins{} wins, no losses, and \selectorComparisonTies{} ties (Table~\ref{tab:pruning}).

\begin{table}[t]
\caption{Matched selector results and diagnostic controls. Accuracy intervals are \confidenceLevel{}\% family-cluster bootstrap intervals.}
\label{tab:pruning}
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lcc@{}}
\toprule
Condition & Accuracy & Count \\
\midrule
Full context
& \pruningFullAccuracy{} [\pruningFullAccuracyLow{}, \pruningFullAccuracyHigh{}]
& \pruningFullCount{} \\
Keyword pruned
& \pruningPrunedAccuracy{} [\pruningPrunedAccuracyLow{}, \pruningPrunedAccuracyHigh{}]
& \pruningPrunedCount{} \\
Query entity
& \entitySelectorAccuracy{} [\entitySelectorAccuracyLow{}, \entitySelectorAccuracyHigh{}]
& \entitySelectorCount{} \\
Decoy removed
& \pruningNoDecoyAccuracy{} [\pruningNoDecoyAccuracyLow{}, \pruningNoDecoyAccuracyHigh{}]
& \pruningNoDecoyCount{} \\
Literal wording
& \literalControlAccuracy{} [\literalControlAccuracyLow{}, \literalControlAccuracyHigh{}]
& \literalControlCount{} \\
Paraphrase wording
& \paraphraseControlAccuracy{} [\paraphraseControlAccuracyLow{}, \paraphraseControlAccuracyHigh{}]
& \paraphraseControlCount{} \\
Closed book
& \closedBookAccuracy{} [\closedBookAccuracyLow{}, \closedBookAccuracyHigh{}]
& \closedBookCount{} \\
\bottomrule
\end{tabular}
}
\end{table}

Removing only the retained decoy raised keyword-pruned accuracy to \pruningNoDecoyAccuracy{}\%, a change of \pruningNoDecoyChange{} points with \pruningNoDecoyWins{} wins and no losses. Literal wording changed matched accuracy by \literalControlChange{} points, with \literalControlWins{} wins and no losses. Across all outputs, distractor capture was \distractorCaptureRate{}\%, exact-output compliance was \exactOutputComplianceRate{}\%, and invalid output was \invalidOutputRate{}\%. The diagnostic and deployable selector agree that retaining the lexical competitor, rather than deleting the answer, caused the keyword filter's failure in this grid.

\section{Limitations}

The findings concern one small quantized model and may not transfer to larger models or other quantization levels. Synthetic clause lookup is not natural contract analysis, and retrieval accuracy is not a measure of general capability. The \cpuBudgetMinutes{}-minute CPU budget capped the grid, leaving only \tierEightCount{} full-context probes at the longest tier and wide intervals throughout. That sparse tier prevents a conclusion about degradation within the measured range, and the experiment cannot address longer prompts. The filler comparison confounds topical relation with semantic domain, entity and code density, candidate count, and sentence templates. A factorial design that matches those properties would be needed to identify a topical-relevance effect. Greedy decoding removes run-to-run sampling variation but does not show how sampling would change the estimates. Query-entity selection assumes that the question names one company exactly and that the relevant sentence repeats that name. It does not address aliases, pronouns, or queries without a unique named subject. The decoy-removal control also depends on an oracle label unavailable in natural documents.

\section{Reproducibility}

Analysis scripts generate every reported quantity from the committed probe manifest and raw model outputs. This procedure follows the principle that reports of computational evidence should expose their budget and variation \citep{dodge2019show}. The model and cached data are frozen and checksummed, and each raw output retains its stable probe identifier. Running \texttt{make all} regenerates \texttt{results.json}, the LaTeX value macros, and the paper from cached outputs; running \texttt{make clean-deep \&\& make all} repeats deterministic model inference. Repository gates check frozen inputs, citation resolution, number provenance, pipeline freshness, result sanity, prose style, anonymity, and compilation.

\label{main-body-end}
\bibliography{refs}
\bibliographystyle{icml2026}

\end{document}
