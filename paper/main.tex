\documentclass{article}

\usepackage{microtype}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}

% Attempt to make hyperref and algorithmic work together better:
\newcommand{\theHalgorithm}{\arabic{algorithm}}

% Submission mode: never add the `accepted` option before the double-blind deadline.
\usepackage{icml2026}

% Macros generated from results.json — the ONLY legal source of numbers.
\InputIfFileExists{values.tex}{}{}
\input{model.tex}

% Keep \smash: the ICML 2026 class's height check suppresses even short capitalized titles.
% The PDF gate rejects "Title Suppressed Due to Excessive Size" if this regresses.
\icmltitlerunning{\smash{Context Position}}

\begin{document}

\twocolumn[
\icmltitle{Does More Context Degrade Model Performance? \\
           Measuring When It Starts and How Much It Costs}
\icmlsetsymbol{equal}{*}
\begin{icmlauthorlist}
\icmlauthor{Anonymous Authors}{anon}
\end{icmlauthorlist}
\icmlaffiliation{anon}{Anonymous Institution}
\icmlkeywords{autonomous research, long context, lost in the middle, small language models}
\vskip 0.3in
]
\printAffiliationsAndNotice{}

% ============================================================================
% AGENT INSTRUCTIONS (delete these comments as sections are written):
% - Write the OUTLINE first (as comments below), then prose from the outline.
% - Every number: a macro from values.tex. Never type digits (gate-enforced).
% - Claims are PREDICTIVE, never causal. Honest limitations. See SPEC.md rubric.
% - Citations: add to refs.bib, then uncomment the bibliography lines below.
% ============================================================================

\begin{abstract}
% TODO(agent): 4-6 sentences — question, data, method, headline result
% (via macros), one-sentence limitation. Written LAST, from real results.
TODO: abstract pending first results.
\end{abstract}

\section{Introduction}
% TODO(agent): the question, why it matters, what we do, contributions list.

\section{Related Work}
% TODO(agent): long-context position effects, input-length degradation,
% irrelevant-context distraction, non-literal retrieval, and Qwen model family.
% Cite only verified entries in refs.bib.

\section{Probe Design}
% TODO(agent): exact frozen model artifact; tokenizer-measured tiers; synthetic
% contract clauses; paired scenario families; independent distractor placement;
% literal/paraphrase questions; relevant/irrelevant filler; deterministic scoring.

\section{Method}
% TODO(agent): answer all four questions in SPEC. Amount tiers need their own n;
% position must compare the same scenario family with only clause location moved;
% quality and pruning contrasts must use matched cases; report exact-output compliance.

\section{Results}
% TODO(agent): >= 2 figures or tables, all values via macros.

\section{Limitations}
% TODO(agent): one small quantized model; synthetic probes, not natural documents;
% retrieval is not general capability; CPU-limited grid; greedy decoding only;
% identify every sparse or unmatched comparison explicitly.

\section{Reproducibility Statement}
% TODO(agent): exact model filename + checksum; raw probe manifest; make all
% regenerates scored results from cached outputs; make clean-deep && make all
% repeats inference; gates enforce frozen inputs, citations, and number provenance.

% Uncomment when refs.bib has verified entries:
\label{main-body-end}
% \bibliography{refs}
% \bibliographystyle{icml2026}

\end{document}
